[buildout]
extends = base.cfg

parts +=
    graph

[Django]
stage = development

[graph]
recipe = zc.recipe.egg:scripts
eggs = ${buildout:eggs}
dependent-scripts = false
entry-points =
    graph=graph:run
